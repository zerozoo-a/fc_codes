import { __read } from "tslib.js";
import take from "./Lazy/take.js";
import pipe from "./pipe.js";
import toArray from "./toArray.js";
import { isAsyncIterable, isIterable } from "./_internal/utils.js";
function head(iterable) {
    if (isIterable(iterable)) {
        return pipe(take(1, iterable), toArray, function (_a) {
            var _b = __read(_a, 1), a = _b[0];
            return a;
        });
    }
    if (isAsyncIterable(iterable)) {
        return pipe(take(1, iterable), toArray, function (_a) {
            var _b = __read(_a, 1), a = _b[0];
            return a;
        });
    }
    throw new TypeError("'iterable' must be type of Iterable or AsyncIterable");
}
export default head;
//# sourceMappingURL=head.js.map