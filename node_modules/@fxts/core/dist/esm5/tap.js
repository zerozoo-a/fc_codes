import { isPromise } from "./_internal/utils.js";
function tap(f, v) {
    if (v === undefined) {
        return function (v) { return tap(f, v); };
    }
    var res = isPromise(v) ? v.then(f) : f(v);
    if (isPromise(res)) {
        return res.then(function () { return v; });
    }
    return v;
}
export default tap;
//# sourceMappingURL=tap.js.map