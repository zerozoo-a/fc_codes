declare type IdentityObject<T> = T extends object ? T : never;
/**
 * Checks if value is the type of object.
 *
 * @example
 * ```ts
 * isObject({}); // true
 * isObject([1, 2, 3]); // true
 * isObject(() => {}); // true
 * isObject(null); // false
 * isObject(123); // false
 * ```
 */
declare function isObject<T = unknown>(a: T): a is IdentityObject<T>;
export default isObject;
//# sourceMappingURL=isObject.d.ts.map