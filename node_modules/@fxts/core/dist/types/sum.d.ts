import ReturnValueType from "./types/ReturnValueType";
/**
 * Adds all the elements of a Iterable/AsyncIterable.
 *
 * @example
 * ```ts
 * sum([1, 2, 3, 4]); // 10
 * sum(['a', 'b', 'c']); // 'abc'
 * await sum(toAsync([1, 2, 3, 4])); // 10
 * await sum(toAsync(['a', 'b', 'c'])); // 'abc'
 * ```
 */
declare function sum<A extends Iterable<number> | AsyncIterable<number> | Iterable<string> | AsyncIterable<string>>(iterable: A): ReturnValueType<A>;
export default sum;
//# sourceMappingURL=sum.d.ts.map